
import SwiftUI

// This is just a helper component to make it easier to start new components as needed. Just copy and paste it into a new file and replace the name of the component and the insides of its body :)

public struct BasicView: View {
    
    public init(){}
    
    public var body : some View {
        Text("Replace me")
    }
}
